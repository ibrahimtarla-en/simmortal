export const VIEWBOX_WIDTH = 100;
export const SIDE_OFFSET = 5;
export const HEIGHT_OFFSET = 5;
export const X_POSITIONS = [
  (VIEWBOX_WIDTH / 4) * 3,
  (VIEWBOX_WIDTH / 4) * 3,
  (VIEWBOX_WIDTH / 4) * 1,
  (VIEWBOX_WIDTH / 4) * 1,
];
