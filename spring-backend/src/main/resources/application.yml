spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/simmortal
    username: simmortal
    password: simmortal
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  mail:
    host: smtp.example.com
    port: 587
    username: user
    password: password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  servlet:
    context-path: /api/v1

app:
  jwt:
    secret: change-me-please-change-me-please
  supertokens:
    connection-uri: http://localhost:3567
    api-key: ""
    anti-csrf: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
